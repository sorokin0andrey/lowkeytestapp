import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cellContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 15,
  },

  labelContainer: {
    flex: 1,
  },

  iconContainer: {
    width: 24,
    marginRight: 15,
  },

  label: {
    fontSize: 14,
  },
})
